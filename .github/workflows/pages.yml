name: Deploy Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write
  
jobs:
  pages:
    uses: groda/gha-workflows/.github/workflows/pages.yml@main
    with:
      deploy-path: "public"